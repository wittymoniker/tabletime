<?php
define('TT_DB_ALLOW_UNCONFIGURED',true);require_once __DIR__.'/db.php';require_once __DIR__.'/schema_compat.php';require_once __DIR__.'/tag_engine.php';require_once __DIR__.'/maintenance.php';require_once __DIR__.'/federation_lib.php';if(function_exists('mysqli_report'))mysqli_report(MYSQLI_REPORT_OFF);
$message='';$error='';$notes=[];$selfcheck=[];$db=null;
if(!$TT_DB_CONFIGURED){$error='No database credentials are visible to Tabletime yet. Attach/create managed MySQL and redeploy.';}
elseif(!class_exists('mysqli')){$error='Database credentials are configured, but PHP mysqli is not loaded.';}
else{$db=@new mysqli($DATABASE_HOST,$DATABASE_USER,$DATABASE_PASS,$DATABASE_NAME);if($db->connect_errno){$error='MySQL connection failed: '.$db->connect_error;}else{$db->set_charset('utf8mb4');try{if(!tt_upgrade_runtime_schema($db,$notes))throw new RuntimeException($db->error?:'runtime schema migration returned false');$missing=tt_verify_accounts_runtime_columns($db);if($missing)throw new RuntimeException('accounts columns still missing: '.implode(', ',$missing));$selfcheck=tt_runtime_sql_selfcheck($db);if($selfcheck)throw new RuntimeException('critical SQL prepare checks failed: '.implode('; ',array_map(fn($k,$v)=>$k.': '.$v,array_keys($selfcheck),$selfcheck)));$writecheck=tt_runtime_write_selfcheck($db);if($writecheck)throw new RuntimeException('critical write/read self-test failed: '.implode('; ',$writecheck));$r=$db->query('SELECT `id`,`tags` FROM `posts`');if($r){while($row=$r->fetch_assoc())tt_index_post_tags($db,(int)$row['id'],(string)$row['tags']);$r->free();}$message='Setup complete. Existing data was preserved, the schema was upgraded in place, and registration/login/post/group/event/forum/message/attachment SQL all prepared successfully.';}catch(Throwable $e){$error='Schema upgrade/self-test failed: '.$e->getMessage();}}}
$tables=0;$size=0;if($db instanceof mysqli&&!$db->connect_errno){if($r=$db->query('SELECT COUNT(*) AS `c` FROM information_schema.tables WHERE table_schema=DATABASE()')){$tables=(int)$r->fetch_assoc()['c'];$r->free();}$size=tt_db_size_bytes($db);}
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tabletime · Database Setup</title><style>body{font:16px system-ui;background:#0d1117;color:#e6edf3;max-width:860px;margin:6vh auto;padding:24px}code{word-break:break-word}.ok{background:#123d2a;padding:12px}.err{background:#4d1717;padding:12px}a{color:#7ee7ff}li{margin:.35rem 0}</style></head><body><h1>Tabletime · Database setup</h1><p><strong>Build:</strong> ESKI-TABLETIME-RUNTIME-COMPAT-2026-09-12</p><?php if($message):?><p class="ok"><?=htmlspecialchars($message,ENT_QUOTES|ENT_SUBSTITUTE,'UTF-8')?></p><?php endif;?><?php if($error):?><p class="err"><?=htmlspecialchars($error,ENT_QUOTES|ENT_SUBSTITUTE,'UTF-8')?></p><?php endif;?><p>Managed database: <strong><?=$db instanceof mysqli&&!$db->connect_errno?'OK':'not connected'?></strong></p><p>Credential source: <strong><?=htmlspecialchars($TT_DB_SOURCE,ENT_QUOTES)?></strong></p><p>Detected tables: <strong><?=$tables?></strong></p><p>Current DB size: <strong><?=number_format($size/1000000,2)?> MB</strong> / 100 MB policy ceiling.</p><?php if($notes):?><details><summary>Migration notes</summary><ul><?php foreach($notes as $n):?><li><?=htmlspecialchars($n,ENT_QUOTES|ENT_SUBSTITUTE,'UTF-8')?></li><?php endforeach;?></ul></details><?php endif;?><?php if(!$error&&$db instanceof mysqli):?><p class="ok"><strong>Runtime SQL + write/read self-test: PASS.</strong> Registration, login, posting, attachments, groups, events and forums were prepared and temporary writes were successfully created, read and cleaned up against this database.</p><?php endif;?><p><a href="register.php">Register</a> · <a href="login.php">Login</a> · <a href="post.php">Posts</a> · <a href="extend.php">Federation</a> · <a href="/">Eski home</a></p></body></html>
