# GitHub release notes — 2026-09-12

This archive is prepared for direct repository/release upload and remains below 25 MB. It contains the current Tabletime application source, SQL schema, federation code, setup/maintenance paths, user themes, and the portable database configuration example.

### Communications update — 2026-09-12
- Persistent renewable login tokens (server stores only SHA-256 token hashes).
- Private, group, and event-scoped WebRTC video calling.
- Browser Notifications API + service worker activity notifications.
- Message/group/event invite notifications.
- Optional TURN relay configuration for restrictive NAT/firewall environments.
