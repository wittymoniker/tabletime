(()=>{const C=window.TT_CALL_CONTEXT||{};const q=s=>document.querySelector(s);const scope=q('#call-scope'),scopeId=q('#call-scope-id'),members=q('#call-members'),priv=q('#private-label'),sl=q('#scope-label');let key=new URLSearchParams(location.search).get('join')||'',stream=null,last=0,timer=null;const pcs=new Map(),names=new Map();function api(a,data={},method='POST'){const fd=new FormData();fd.set('action',a);for(const[k,v]of Object.entries(data))fd.set(k,v);if(method==='GET')return fetch('api/call_signal.php?'+new URLSearchParams({action:a,...data}),{credentials:'same-origin',cache:'no-store'}).then(r=>r.json());return fetch('api/call_signal.php',{method:'POST',body:fd,credentials:'same-origin'}).then(r=>r.json());}function fill(){const t=scope.value,x=t==='group'?C.groups:t==='event'?C.events:[];scopeId.innerHTML=x.map(v=>`<option value="${v.id}">${esc(v.title)}</option>`).join('');const privateMode=t==='private';scopeId.hidden=sl.hidden=privateMode;members.hidden=priv.hidden=!privateMode;}function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}scope.addEventListener('change',fill);fill();q('#call-create').addEventListener('submit',async e=>{e.preventDefault();const d=await api('create',{scope_type:scope.value,scope_id:scopeId.value||0,members:members.value,title:q('#call-title').value});if(d.error)return status(d.error);key=d.call_key;history.replaceState(null,'','calls.php?join='+key);await join();});function status(s){const n=q('#call-status');n.hidden=false;n.textContent=s;}async function media(){if(stream)return stream;stream=await navigator.mediaDevices.getUserMedia({video:true,audio:true});q('#local-video').srcObject=stream;return stream;}async function pc(peer,username,initiator=false){peer=Number(peer);if(peer===Number(C.uid)||pcs.has(peer))return pcs.get(peer);await media();names.set(peer,username||('User '+peer));const p=new RTCPeerConnection({iceServers:C.iceServers||[{urls:'stun:stun.l.google.com:19302'}]});pcs.set(peer,p);for(const tr of stream.getTracks())p.addTrack(tr,stream);p.onicecandidate=e=>{if(e.candidate)api('signal',{call_key:key,to:peer,signal_type:'ice',payload:JSON.stringify(e.candidate)});};p.ontrack=e=>remote(peer,e.streams[0]);p.onconnectionstatechange=()=>{if(['failed','closed','disconnected'].includes(p.connectionState))setTimeout(()=>remove(peer),3000);};if(initiator){const o=await p.createOffer();await p.setLocalDescription(o);await api('signal',{call_key:key,to:peer,signal_type:'offer',payload:JSON.stringify(o)});}return p;}function remote(id,s){let v=document.getElementById('peer-'+id);if(!v){const f=document.createElement('figure');v=document.createElement('video');v.id='peer-'+id;v.autoplay=true;v.playsInline=true;const c=document.createElement('figcaption');c.textContent=names.get(Number(id))||'Participant';f.append(v,c);q('#remote-videos').appendChild(f);}v.srcObject=s;}function remove(id){const p=pcs.get(Number(id));if(p){p.close();pcs.delete(Number(id));}document.getElementById('peer-'+id)?.parentElement?.remove();}async function signals(){const d=await api('poll',{call_key:key,after:last},'GET');for(const s of d.signals||[]){last=Math.max(last,Number(s.id)||0);const from=Number(s.from_account_id);names.set(from,s.username);let p=pcs.get(from);if(s.signal_type==='offer'){p=await pc(from,s.username,false);await p.setRemoteDescription(JSON.parse(s.payload));const a=await p.createAnswer();await p.setLocalDescription(a);await api('signal',{call_key:key,to:from,signal_type:'answer',payload:JSON.stringify(a)});}else if(s.signal_type==='answer'&&p){await p.setRemoteDescription(JSON.parse(s.payload));}else if(s.signal_type==='ice'&&p){try{await p.addIceCandidate(JSON.parse(s.payload));}catch(_){}}}}async function beat(){const d=await api('heartbeat',{call_key:key});if(d.error)return status(d.error);for(const x of d.peers||[]){if(Number(x.id)!==Number(C.uid)&&!pcs.has(Number(x.id))&&Number(C.uid)<Number(x.id))pc(x.id,x.username,true);}await signals();}async function join(){try{await media();const d=await api('join',{call_key:key});if(d.error)throw Error(d.error);q('#call-room').hidden=false;q('#call-heading').textContent=d.call?.title||'Call';q('#call-create').closest('.card').hidden=true;for(const x of d.peers||[])if(Number(x.id)!==Number(C.uid)&&Number(C.uid)<Number(x.id))pc(x.id,x.username,true);timer=setInterval(beat,2500);status('Connected to call.');}catch(e){status(e.message||String(e));}}q('#call-camera').onclick=()=>stream?.getVideoTracks().forEach(t=>t.enabled=!t.enabled);q('#call-mic').onclick=()=>stream?.getAudioTracks().forEach(t=>t.enabled=!t.enabled);q('#call-share').onclick=async()=>{try{const ds=await navigator.mediaDevices.getDisplayMedia({video:true});const tr=ds.getVideoTracks()[0];for(const p of pcs.values()){const snd=p.getSenders().find(s=>s.track?.kind==='video');if(snd)await snd.replaceTrack(tr);}tr.onended=async()=>{const cam=stream?.getVideoTracks()[0];for(const p of pcs.values()){const snd=p.getSenders().find(s=>s.track?.kind==='video');if(snd&&cam)await snd.replaceTrack(cam);}};}catch(_){}};q('#call-leave').onclick=async()=>{clearInterval(timer);await api('leave',{call_key:key});for(const p of pcs.values())p.close();stream?.getTracks().forEach(t=>t.stop());location.href='calls.php';};if(key)join();})();